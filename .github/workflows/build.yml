name: Build & Test Rentities 1.21.11

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repository code
      - name: Checkout Repository
        uses: actions/checkout@v4

      # Step 2: Set up JDK 21
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      # Step 3: Setup Gradle
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          cache-read-only: false

      # Step 4: Force Gradle 9.4.0 to match Loom 1.16.1 requirements
      - name: Repair Gradle Wrapper
        run: gradle wrapper --gradle-version 9.4.0

      # Step 5: Grant execute permission for Gradle wrapper
      - name: Make gradlew Executable
        run: chmod +x ./gradlew

      # Step 6: Build project
      - name: Build with Gradle
        run: ./gradlew build --no-daemon

      # Step 7: Upload build artifacts (.jar files)
      - name: Upload Mod Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: rentities-1.21.11-build
          path: build/libs/*.jar
          retention-days: 7
